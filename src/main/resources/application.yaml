spring:
  profiles:
    active: root
  datasource:
    url: jdbc:mysql://localhost:3306/mirsal
    username: root
    password: afnansql1302
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080


